/**
 * @requires tent
 * @requires tent.changes
 * @requires tent.entities
 * @requires tent.logging
 * @name tent.connectors
 * @namespace Container namespace for connectors that load items and persist changes in a {@link tent.entities.Context} to different data targets (ie: RESTful services, Html Isolated Storage, SQL, FTP, etc.)
 */
tent.declare('tent.connectors', function(){

});