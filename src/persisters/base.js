/**
 * @requires tent
 * @requires tent.changes
 * @requires tent.entities
 * @requires tent.logging
 * @name tent.persisters
 * @namespace Container namespace for connectors that persist changes in a {@link tent.entities.Context} to different data targets (ie: RESTful services, Html Isolated Storage, SQL, FTP, etc.)
 */
tent.declare('tent.persisters', function(){

});